let x = 3
let y = 2 * x
fn calculate_sum(a: Int, b: Int) -> Int:
    if a > b:
        return a + b
    else:
        return b - a
print(calculate_sum(x, y))
let power2 = |n: Int| -> Int: 2 ** n
print(power2(3))
